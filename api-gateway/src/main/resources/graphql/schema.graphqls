type Query {
    orders: [Order]
}

type Order {
    id: ID!
    quantity: Int
    status: String
    user: User
    product: Product
}

type User {
    id: ID!
    name: String
    email: String
}

type Product {
    id: ID!
    name: String
    description: String
    price: Float
    stock: Int
}