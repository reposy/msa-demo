spring:
  application:
    name: product-service
  r2dbc:
    url: r2dbc:postgresql://postgres-product:5432/productdb
    username: product
    password: secret
    initialize: true
    initialization-mode: always

server:
  port: 8082