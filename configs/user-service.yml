spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: r2dbc:postgresql://postgres-user:5432/userdb
    username: user
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083